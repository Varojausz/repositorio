git init

touch secreto.txt

vim .ignoregit

git status

git add -A

git commit "Mis primeros archivos"

git log

git clone

git remote add origin https://github.com/Varojausz/repositorio

git push -u origin master

git branch minuevafeature

git checkout minuevafeature

git branch

git add -A

git commit -m "agregando mas cosas"

git push -u origin minuevafeature

git branch -a

git checkout master

probando merge

git push origin --delete minuevafeature